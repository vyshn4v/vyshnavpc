import { appendDsaTopics } from './dsa-seeder-utils.js';

const topics = [
  {
    slug: "dsa-ch56-greedy-algorithms",
    title: "Chapter 56: Greedy Algorithms - Local Optima to Global Optima",
    order: 56,
    content: "<h2>Taking What's Right in Front of You</h2><p>A Greedy Algorithm builds up a solution piece by piece, always choosing the next piece that offers the most immediate benefit (the local optimum). The defining characteristic of a successful greedy algorithm is the <strong>Greedy Choice Property</strong>: making a locally optimal choice at each step must mathematically lead to a globally optimal solution.</p><h3>When does Greedy fail?</h3><p>Greedy algorithms fail when making a short-term optimal choice traps you in a sub-optimal long-term path. For example, in the Coin Change problem with coin denominations [1, 3, 4] and a target of 6, a greedy approach picks 4, leaving 2, which requires two 1s (total 3 coins). The optimal solution is picking 3 twice (total 2 coins). Here, DP is required instead.</p><h3>Example: Fractional Knapsack</h3><p>Unlike 0/1 Knapsack where you must take an item whole, in Fractional Knapsack you can take fractions of items. This transforms a complex DP problem into a trivial Greedy problem. We simply calculate the `value/weight` ratio for each item, sort them descending, and greedily take as much of the highest ratio items as possible.</p><pre><code>function fractionalKnapsack(items, capacity) {\n  // items = [{ weight: x, value: y }, ...]\n  items.sort((a, b) => (b.value / b.weight) - (a.value / a.weight));\n  let totalValue = 0;\n  for (let item of items) {\n    if (capacity >= item.weight) {\n      totalValue += item.value;\n      capacity -= item.weight;\n    } else {\n      totalValue += item.value * (capacity / item.weight);\n      break;\n    }\n  }\n  return totalValue;\n}\n// Time Complexity: O(N log N) dominated by sorting.\n// Space Complexity: O(1) or O(N) depending on sort implementation.</code></pre>",
    interviewQuestions: [
      { question: "How do you prove a Greedy algorithm is correct?", answer: "Usually via an 'exchange argument'. You assume there is an optimal solution that differs from the greedy solution. You then show that exchanging the choices in the optimal solution with the greedy choices either improves or maintains the optimality, proving the greedy strategy is strictly non-worse." },
      { question: "What is the primary difference between Greedy and Dynamic Programming?", answer: "At each step, a Greedy algorithm makes a single irrevocable choice. DP evaluates all possible choices recursively (or iteratively) to find the absolute best, handling overlapping subproblems." },
      { question: "Can Dijkstra's Algorithm be considered a Greedy Algorithm?", answer: "Yes! Dijkstra's iteratively selects the unvisited node with the smallest tentative distance (a locally optimal greedy choice) to secure the globally optimal shortest path. However, it fails with negative weight edges, demonstrating where the greedy choice property breaks down." },
      { question: "Why does standard Coin Change work greedily for US Currency but not arbitrarily?", answer: "US coin denominations (1, 5, 10, 25) form a 'Canonical Coin System'. The denominations are structured such that greedy selection is mathematically guaranteed to use the fewest coins. Arbitrary systems (like 1, 3, 4) break this mathematical property." },
      { question: "What is the Time Complexity of most Greedy algorithms?", answer: "Typically O(N log N). Because making the 'best' local choice usually requires evaluating elements in a specific order, sorting the data first is the dominant operation." }
    ],
    practicalTask: {
      scenario: "Jump Game.",
      task: "You are given an integer array nums. You are initially positioned at the array's first index, and each element in the array represents your maximum jump length at that position. Return true if you can reach the last index. Use a greedy approach.",
      solutionCode: "function canJump(nums) {\n  let goal = nums.length - 1;\n  // Iterate backwards. Can we reach the goal from i?\n  for (let i = nums.length - 2; i >= 0; i--) {\n    if (i + nums[i] >= goal) {\n      goal = i; // Shift the goalpost closer to the start\n    }\n  }\n  return goal === 0;\n}\n// Time: O(N), Space: O(1)"
    }
  },
  {
    slug: "dsa-ch57-greedy-intervals",
    title: "Chapter 57: Advanced Greedy Strategies & Interval Problems",
    order: 57,
    content: "<h2>Taming Chaos: Intervals</h2><p>Interval problems (like Meeting Rooms, Merge Intervals, Non-overlapping Intervals) form a massive subcategory of FAANG interview questions. Nearly all interval problems can be solved cleanly by combining Sorting with a Greedy Strategy.</p><h3>The Golden Rule of Intervals</h3><p>If you need to <strong>merge</strong> intervals, sort by the <strong>start time</strong>. If you need to find the <strong>maximum non-overlapping</strong> intervals (scheduling), sort by the <strong>end time</strong>.</p><h3>Example: Merge Intervals</h3><p>Given an array of intervals where intervals[i] = [starti, endi], merge all overlapping intervals.</p><h4>Brute Force (O(N^2))</h4><p>Compare every interval with every other interval and merge them. Highly inefficient.</p><h4>Optimized Greedy (O(N log N))</h4><pre><code>function mergeIntervals(intervals) {\n  if (intervals.length <= 1) return intervals;\n  \n  // 1. Sort by start time\n  intervals.sort((a, b) => a[0] - b[0]);\n  \n  const result = [intervals[0]];\n  \n  for (let i = 1; i < intervals.length; i++) {\n    const current = intervals[i];\n    const lastMerged = result[result.length - 1];\n    \n    // 2. Overlap detected? Maximize the end time.\n    if (current[0] <= lastMerged[1]) {\n      lastMerged[1] = Math.max(lastMerged[1], current[1]);\n    } else {\n      // 3. No overlap, push to result\n      result.push(current);\n    }\n  }\n  return result;\n}\n// Time Complexity: O(N log N) for sorting.\n// Space: O(N) for output array.</code></pre>",
    interviewQuestions: [
      { question: "Why do we sort by END time for 'Activity Selection' or 'Non-overlapping Intervals'?", answer: "If you want to pack as many activities as possible into a schedule, your greedy choice should be to always pick the activity that finishes first, leaving the maximum possible remaining time for other activities." },
      { question: "How do you solve the 'Meeting Rooms II' problem (finding the minimum number of conference rooms required)?", answer: "You use a Sweep Line algorithm or two pointers. Separate start times and end times into two sorted arrays. Iterate with two pointers. If a start time is earlier than the next end time, a new room is needed (increment counter). Otherwise, a meeting ended, so a room is freed (decrement counter)." },
      { question: "What is a Sweep Line Algorithm?", answer: "It's a greedy geometric technique where a conceptual 'line' sweeps across a coordinate system (often time) from left to right. Events (like an interval starting `+1` or ending `-1`) are processed in chronological order to track the active state at any given point." },
      { question: "Can interval merging be done in O(N) time if the data is already sorted?", answer: "Yes. If the intervals are guaranteed to be sorted by start time, the greedy linear pass takes exactly O(N) time and O(1) auxiliary space (excluding the output array)." },
      { question: "In merge intervals, why do we use `Math.max(lastMerged[1], current[1])` instead of just assigning `current[1]`?", answer: "Because interval `A` could completely swallow interval `B`. E.g., `[1, 10]` and `[2, 5]`. If we just took `B`'s end time, the merged interval would shrink to `[1, 5]`, which is incorrect. We need `[1, 10]`." }
    ],
    practicalTask: {
      scenario: "Non-overlapping Intervals.",
      task: "Given an array of intervals, return the minimum number of intervals you need to remove to make the rest of the intervals non-overlapping. Use the greedy end-time sorting strategy.",
      solutionCode: "function eraseOverlapIntervals(intervals) {\n  if (intervals.length === 0) return 0;\n  // Sort by END time\n  intervals.sort((a, b) => a[1] - b[1]);\n  \n  let removals = 0;\n  let currentEnd = intervals[0][1];\n  \n  for (let i = 1; i < intervals.length; i++) {\n    if (intervals[i][0] < currentEnd) {\n      removals++; // Overlaps! Remove it.\n    } else {\n      currentEnd = intervals[i][1]; // Update end time to current valid interval\n    }\n  }\n  return removals;\n}\n// Time: O(N log N)"
    }
  },
  {
    slug: "dsa-ch58-divide-conquer-master",
    title: "Chapter 58: Divide & Conquer I: The Master Theorem in Practice",
    order: 58,
    content: "<h2>Dividing the Workload</h2><p>Divide and Conquer (D&C) is an algorithmic paradigm that breaks a problem into smaller, non-overlapping subproblems of the same type, solves them recursively, and then combines their results. It powers some of the most efficient algorithms in computer science.</p><h3>The Three Steps</h3><ol><li><strong>Divide:</strong> Break the problem into `a` subproblems, each of size `n/b`.</li><li><strong>Conquer:</strong> Solve the subproblems recursively.</li><li><strong>Combine:</strong> Merge the answers in `O(n^d)` time.</li></ol><h3>The Master Theorem</h3><p>The Master Theorem gives us a direct formula to solve recurrence relations of the form: <code>T(n) = a*T(n/b) + O(n^d)</code>.</p><ul><li>If <code>a > b^d</code>: The work is dominated by the leaves (subproblems). Time: <code>O(n^(log_b(a)))</code>.</li><li>If <code>a === b^d</code>: The work is evenly distributed. Time: <code>O(n^d * log n)</code>.</li><li>If <code>a < b^d</code>: The work is dominated by the root (combine step). Time: <code>O(n^d)</code>.</li></ul><h3>Example: Binary Search Analysis</h3><p>In Binary Search, we divide into 1 subproblem (<code>a=1</code>) of half the size (<code>b=2</code>). The combine step is just returning a value (<code>d=0</code>).<br><code>a = 1, b^d = 2^0 = 1</code>.<br>Since <code>a === b^d</code>, Time is <code>O(n^0 * log n) = O(log n)</code>.</p>",
    interviewQuestions: [
      { question: "Why is Merge Sort O(N log N)? Explain using the Master Theorem.", answer: "Merge sort divides into 2 subproblems (a=2), each half size (b=2). The merging takes O(N) linear time (d=1). Since 2 === 2^1, it falls in the balanced case. Thus, Time is O(N^1 * log N) = O(N log N)." },
      { question: "How does Divide & Conquer fundamentally differ from Dynamic Programming?", answer: "D&C subproblems are independent and non-overlapping. A subproblem `f(n/2)` does not share calculations with the other `f(n/2)`. DP relies on overlapping subproblems, calculating the same state repeatedly unless cached." },
      { question: "What is the time complexity of Strassen's Matrix Multiplication algorithm?", answer: "O(N^2.81). Standard matrix multiplication is O(N^3). Strassen used D&C to break matrices into 8 multiplications, but via clever algebra reduced it to 7 (a=7, b=2, d=2). Since 7 > 2^2, time is O(N^(log_2(7))) ≈ O(N^2.81)." },
      { question: "Is Divide and Conquer always recursive?", answer: "Most naturally, yes. However, any recursive D&C algorithm can theoretically be written iteratively using an explicit stack (or bottom-up loops, like iterative Merge Sort), though it is often much harder to read." },
      { question: "What is a 'Divide and Conquer Optimization' in DP?", answer: "It is an advanced technique used to optimize DP transitions of the form `dp[i][j] = min(dp[i-1][k] + cost(k, j))`. If the optimal `k` is monotonic (moves in one direction as `j` increases), we can use D&C to find `k` in O(log N) instead of O(N)." }
    ],
    practicalTask: {
      scenario: "Majority Element (Divide & Conquer Approach).",
      task: "Given an array of size n, return the majority element. While Boyer-Moore voting is O(N), implement the D&C approach to understand the tree structure.",
      solutionCode: "function majorityElementDC(nums) {\n  const getMajority = (left, right) => {\n    if (left === right) return nums[left];\n    \n    let mid = Math.floor((left + right) / 2);\n    let leftMaj = getMajority(left, mid);\n    let rightMaj = getMajority(mid + 1, right);\n    \n    if (leftMaj === rightMaj) return leftMaj;\n    \n    let leftCount = 0, rightCount = 0;\n    for (let i = left; i <= right; i++) {\n      if (nums[i] === leftMaj) leftCount++;\n      else if (nums[i] === rightMaj) rightCount++;\n    }\n    \n    return leftCount > rightCount ? leftMaj : rightMaj;\n  };\n  return getMajority(0, nums.length - 1);\n}\n// Time Complexity: O(N log N)"
    }
  },
  {
    slug: "dsa-ch59-divide-conquer-advanced",
    title: "Chapter 59: Divide & Conquer II: Advanced Sorting & Variations",
    order: 59,
    content: "<h2>Beyond the Basics</h2><p>While Merge Sort is the textbook D&C algorithm, the paradigm is used extensively in advanced computational geometry and optimization problems. Quick Sort is another classic, though its 'division' phase (partitioning) is where the heavy lifting occurs, unlike Merge Sort where 'conquering' (merging) takes the work.</p><h3>Quick Sort: The Unbalanced Divider</h3><p>Quick Sort selects a 'pivot' and partitions the array such that smaller elements are on the left and larger on the right. It then recursively Quick Sorts the partitions. The worst-case happens when the array is already sorted, and the pivot is the largest/smallest element, reducing `b` to roughly 1 instead of 2. This degrades the complexity to O(N^2).</p><h3>The Closest Pair of Points</h3><p>Given N points on a 2D plane, find the pair with the smallest Euclidean distance. Brute force is O(N^2). D&C can do it in O(N log N):</p><ol><li>Sort points by X-coordinate.</li><li>Divide into left and right halves. Recursively find min distance `d` in both.</li><li>The trick: The closest pair might span across the middle divide. Create a 'strip' of width `2d` around the middle. Check points inside this strip. Because of geometric constraints, we only need to check at most 7 points for each point in the strip!</li></ol><h3>Example: Merge Sort</h3><pre><code>function mergeSort(arr) {\n  if (arr.length <= 1) return arr;\n  const mid = Math.floor(arr.length / 2);\n  const left = mergeSort(arr.slice(0, mid));\n  const right = mergeSort(arr.slice(mid));\n  return merge(left, right);\n}\n\nfunction merge(left, right) {\n  let res = [], i = 0, j = 0;\n  while (i < left.length && j < right.length) {\n    if (left[i] < right[j]) res.push(left[i++]);\n    else res.push(right[j++]);\n  }\n  return res.concat(left.slice(i)).concat(right.slice(j));\n}\n// Time: O(N log N), Space: O(N)</code></pre>",
    interviewQuestions: [
      { question: "Why is Quick Sort often preferred over Merge Sort in practice, despite having a worse worst-case complexity?", answer: "Quick Sort is an 'in-place' sort (requiring O(log N) stack space, but no O(N) auxiliary arrays like Merge Sort). Furthermore, its tight inner loop has highly contiguous memory access, resulting in excellent CPU cache performance." },
      { question: "How do you mitigate the O(N^2) worst-case in Quick Sort?", answer: "By using randomized pivot selection, or using the 'Median of Three' rule (picking the median of the first, middle, and last elements as the pivot). This practically guarantees an O(N log N) runtime." },
      { question: "Can Merge Sort be done in-place?", answer: "Standard Merge Sort is not in-place because combining two arrays requires a temporary buffer. There are complex algorithms for In-Place Merge Sort, but they usually carry large constant time overheads, making them impractical compared to Quick Sort." },
      { question: "What is the 'Quick Select' algorithm?", answer: "Quick Select is a D&C algorithm used to find the k-th smallest/largest element in an unordered list. It works exactly like Quick Sort partitioning, but only recurses into the half that contains the k-th element. Average time is O(N)." },
      { question: "Why does the Closest Pair 'strip' checking only require checking 7 points?", answer: "It's a mathematical proof based on packing. If the minimum distance found so far is `d`, we are only checking a box of size `2d x d`. You can only fit at most 8 points in this box such that all points are at least distance `d` apart from each other. Thus, the inner loop runs in O(1) time." }
    ],
    practicalTask: {
      scenario: "Sort an Array (Optimal).",
      task: "Implement Quick Sort iteratively or recursively with randomized pivot to sort an array in O(N log N) average time.",
      solutionCode: "function quickSort(arr, left = 0, right = arr.length - 1) {\n  if (left < right) {\n    let pivotIndex = partition(arr, left, right);\n    quickSort(arr, left, pivotIndex - 1);\n    quickSort(arr, pivotIndex + 1, right);\n  }\n  return arr;\n}\n\nfunction partition(arr, left, right) {\n  let pivot = arr[right]; // Note: in production, pick randomly & swap to end\n  let i = left - 1;\n  for (let j = left; j < right; j++) {\n    if (arr[j] < pivot) {\n      i++;\n      [arr[i], arr[j]] = [arr[j], arr[i]];\n    }\n  }\n  [arr[i + 1], arr[right]] = [arr[right], arr[i + 1]];\n  return i + 1;\n}"
    }
  },
  {
    slug: "dsa-ch60-hybrid-approaches",
    title: "Chapter 60: Bringing it All Together - Hybrid Approaches",
    order: 60,
    content: "<h2>The Final Boss: Multi-Paradigm Problems</h2><p>In FAANG onsite interviews, especially for Staff/Senior roles, you will rarely get a pure 'just apply DP' problem. The hardest problems require chaining paradigms together. You might need to use Greedy to sort the data, D&C to break it up, and DP with Memoization to solve the chunks.</p><h3>DP on Trees (Backtracking + DP)</h3><p>Many tree problems require traversing the tree (DFS/Backtracking) while simultaneously memoizing the state at each node to prevent recalculating subtrees. The state is usually the node reference and a boolean condition.</p><h3>Binary Search the Answer (Greedy + D&C)</h3><p>Some optimization problems ask for the 'minimum maximum' or 'maximum minimum'. The array might not be sorted, so standard Binary Search fails. The trick: Binary search the <em>value domain</em> of the answer, and use a Greedy algorithm to check if that answer is valid.</p><h3>Example: Koko Eating Bananas</h3><p>Koko can eat bananas at speed `K` per hour. We want the minimum `K` to eat all bananas within `H` hours. We binary search the possible speeds from `1` to `max(piles)`. For each `K`, we greedily calculate how many hours it takes. If hours <= `H`, `K` is valid, try a smaller `K`.</p><pre><code>function minEatingSpeed(piles, h) {\n  let left = 1, right = Math.max(...piles);\n  let result = right;\n  \n  while (left <= right) {\n    let k = Math.floor((left + right) / 2);\n    let hours = 0;\n    for (let p of piles) hours += Math.ceil(p / k);\n    \n    if (hours <= h) {\n      result = k; // Valid, but try to find a slower speed\n      right = k - 1;\n    } else {\n      left = k + 1; // Too slow, increase speed\n    }\n  }\n  return result;\n}\n// Time: O(N log M) where M is max(piles).\n// Space: O(1)</code></pre>",
    interviewQuestions: [
      { question: "What is 'DP with Bitmasking'?", answer: "A hybrid technique where the DP state relies on tracking a set of used/unused items. Instead of using an array or Set as a state key, we use an integer where each bit represents an item (1=used, 0=unused). This turns state lookup into an O(1) array access." },
      { question: "How do you identify a 'Binary Search the Answer' problem?", answer: "Look for keywords like 'Find the minimum possible maximum value' or 'Minimize the maximum distance'. Also, check if verifying a given target answer is easy (often linear time), but generating the optimal answer directly is hard." },
      { question: "What happens when you combine A* Search with DP?", answer: "You get highly optimal pathfinding. A* uses a heuristic (Greedy choice) to guide the search direction, while maintaining a visited cost map (DP/Memoization) to ensure you never re-explore a node with a worse cost." },
      { question: "Is Timsort a hybrid algorithm?", answer: "Yes! Timsort (the default sorting algorithm in Python and V8 JavaScript) is a hybrid of Merge Sort (Divide & Conquer) and Insertion Sort. It uses Insertion Sort on small chunks, then Merges them together, utilizing real-world data patterns." },
      { question: "How do you mentally transition between Paradigms during an interview?", answer: "Start with Brute Force (Backtracking). Ask: Are there overlapping states? Yes -> DP (Memo). No -> Can we solve halves independently? Yes -> D&C. Is there a mathematical local best choice? Yes -> Greedy. If stuck, try sorting the data first." }
    ],
    practicalTask: {
      scenario: "House Robber III (DP on Trees).",
      task: "The thief has found a new place to rob: a binary tree. If two directly-linked nodes are broken into on the same night, the police are alerted. Return the max money. This requires a hybrid DFS (Backtracking) and DP approach.",
      solutionCode: "function robTree(root) {\n  // Returns an array: [max_with_node, max_without_node]\n  const dfs = (node) => {\n    if (!node) return [0, 0];\n    \n    const left = dfs(node.left);\n    const right = dfs(node.right);\n    \n    // If we rob this node, we CANNOT rob children\n    const withNode = node.val + left[1] + right[1];\n    \n    // If we skip this node, we CAN rob or skip children (take max)\n    const withoutNode = Math.max(left[0], left[1]) + Math.max(right[0], right[1]);\n    \n    return [withNode, withoutNode];\n  };\n  \n  const res = dfs(root);\n  return Math.max(res[0], res[1]);\n}\n// Time: O(N) where N is number of nodes.\n// Space: O(H) for call stack depth."
    }
  }
];

appendDsaTopics(topics);
