<nav class="nav" id="nav">
  <a href="#home" class="nav-brand">
    <span class="dot"></span>
    <span>{{site.name}}</span>
  </a>
  <div class="nav-links">
    {{#each nav}}
    <a href="{{this.href}}"{{#if this.active}} class="active"{{/if}}>{{this.label}}</a>
    {{/each}}
    <a href="{{site.github}}" class="nav-cta" target="_blank" rel="noopener">GitHub ↗</a>
  </div>
  <button class="nav-toggle" id="navToggle" aria-label="Menu">
    <span></span><span></span><span></span>
  </button>
</nav>
