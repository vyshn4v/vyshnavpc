<div class="premium-projects-section" id="projects">
  <div class="premium-card">
    <h2 class="premium-card-title">Projects</h2>
    
    <div class="projects-grid">
      {{#each projects}}
        {{> project-card}}
      {{/each}}
    </div>
    
  </div>
</div>
