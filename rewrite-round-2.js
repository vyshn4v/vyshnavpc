import { rewriteAnswers } from './rewriteAnswers.js';

const map = {
  "In Neutrinos, you architected a stage-based decision API to manage complex UI flows. How did you design the React components to dynamically render these different stage-based UIs without tightly coupling the frontend logic to specific stages?": "When building the stage-based decision UI at Neutrinos, I completely decoupled the frontend from specific business stages. I built a configuration-driven React architecture using a global component registry. The backend API returned a payload specifying the 'stageType', which my dynamic renderer component used to dynamically import and render the correct React component at runtime. This meant if product added a 'review' stage, we just added it to the registry without touching the core routing or state logic. I relied heavily on Higher-Order Components and render props to pass down standard navigation controls and form states to these dynamically rendered components.",
  "How does React's reconciliation algorithm (Fiber) work, and how did you leverage this knowledge to ensure smooth rendering in the Domain Scanner's dynamic dashboards?": "React Fiber is a complete rewrite of React's core algorithm that allows rendering work to be chunked, paused, and prioritized. Under the hood, it builds a linked list of 'fiber nodes' corresponding to the component tree. When I was building Domain Scanner's dynamic dashboards, we had lists of thousands of constantly updating DNS records. If React tried to diff the virtual DOM synchronously, the main thread would freeze. I leveraged my understanding of Fiber by aggressively using unique, stable `key` props on every list item, so React's diffing engine could quickly identify moved versus new elements, skipping expensive sub-tree renders. I also deferred non-critical dashboard updates using concurrent features so typing in search bars remained buttery smooth at 60fps.",
  "For role-specific, context-aware decision paths in Neutrinos, how did you handle component composition and avoiding prop drilling before implementing a global state?": "Before we introduced Redux, prop drilling was a massive issue in Neutrinos because the user's role and decision context had to reach deep into nested form fields. To solve this, I heavily utilized the React Context API paired with the Compound Component pattern. I created a `<DecisionFlow.Provider>` at the top level to hold the role context. Then, instead of passing data down five levels, I used `children` props to pass pre-configured UI components down. For example, `<DecisionFlow.AdminView>` would consume the context directly using `useContext`, meaning intermediate layout components didn't even know the context existed.",
  "Explain the concept of closures in React hooks. What are some common bugs related to stale closures you might have encountered while building the dynamic dashboards for Domain Scanner?": "In JavaScript, a closure is when a function 'remembers' the variables from its lexical scope. In React, functional components render as closures. Every time the component renders, hooks like `useEffect` or `useCallback` capture the state exactly as it was during that specific render. In Domain Scanner, I encountered a nasty bug where a `setInterval` inside a `useEffect` was polling data and trying to append it to an array state, but it kept resetting the array. It was a stale closure because the interval callback was trapped in the very first render's scope where the array was empty. I fixed it by either adding the array to the dependency array (which resets the interval) or, better yet, using the functional updater pattern: `setRecords(prev => [...prev, newData])`, which guarantees access to the latest state.",
  "Can you explain the difference between useMemo and useCallback? In what specific scenarios in Domain Scanner would you use them to optimize rendering, and when might they actually hurt performance?": "Both hooks return memoized values, but `useMemo` caches the result of a calculation, whereas `useCallback` caches the function reference itself. In Domain Scanner, we had a heavy `<Chart>` component that took a parsed dataset and an `onPointClick` handler. I wrapped the `<Chart>` in `React.memo`. To prevent the chart from re-rendering every time the parent rendered, I used `useMemo` for the heavy data parsing logic, and `useCallback` for the `onPointClick` function so its reference remained stable. However, I've seen developers wrap every single function in `useCallback`. This actually hurts performance because React has to do extra work allocating memory for the dependency arrays and running equality checks on every render, which is slower than just recreating a simple inline function.",
  "Given the complex application state logic in Neutrinos, how did you structure your Redux Toolkit slices? Did you normalize your state, and why?": "In Neutrinos, our state was highly relational—users belonged to roles, which had permissions, which mapped to specific decision workflows. Initially, the state was deeply nested, meaning a single update forced us to spread multiple levels deep, causing bugs and slow renders. I restructured our Redux Toolkit slices using feature-based architecture and strictly normalized the state, exactly like a relational database. I used `createEntityAdapter` from RTK to store items flat in a dictionary `entities: { id: { ... } }` and an array of `ids`. This meant when a specific decision node updated, I only updated it by its ID in one place, avoiding nested clones and preventing completely unrelated components from re-rendering.",
  "In Domain Scanner, you integrated Redis and PostgreSQL for backend data. How did you handle asynchronous state and caching on the React frontend using Redux Toolkit?": "For asynchronous state tied to our Postgres and Redis backends, I relied heavily on RTK Query. Before RTK Query, I would manually write `createAsyncThunk` and manage `isLoading`, `isSuccess`, and `isError` flags in the slice. RTK Query automated all of that. I defined API endpoints in a single file, and it auto-generated React hooks like `useGetScansQuery`. Crucially, because Domain Scanner required real-time-ish updates, I utilized RTK Query's automated cache invalidation via tags. When a user submitted a new scan, the mutation would invalidate the 'Scans' tag, instantly triggering a background refetch for the list, keeping the UI perfectly in sync with the backend cache.",
  "Redux can sometimes introduce boilerplate. How does Redux Toolkit simplify this, and what are the key differences between createSlice and traditional Redux reducers?": "Traditional Redux was notoriously verbose: you needed separate files for constants, action creators, and switch-statement reducers, plus you couldn't mutate state. Redux Toolkit eliminates this. The biggest game-changer is `createSlice`. It accepts an object of reducer functions and automatically generates the action creators and action types for you based on the function names. Furthermore, `createSlice` wraps your reducers in Immer.js, meaning I could write code that *looks* like a mutation—like `state.user.name = 'John'`—and Immer safely translates it into an immutable update under the hood, completely eliminating the deeply nested spread operator boilerplate.",
  "How do you handle side effects in a Redux Toolkit architecture when dealing with complex multi-stage workflows like in Neutrinos?": "While RTK thunks are great for simple API calls, complex multi-stage workflows—like waiting for an API response, firing an analytics event, waiting 3 seconds, and redirecting—require more robust side-effect management. In Neutrinos, I championed the use of RTK Listener Middleware. It’s essentially a modern, lightweight alternative to Redux Saga. It allows you to 'listen' for specific action types or state changes. When a 'StageCompleted' action was dispatched, my listener would intercept it, read the next stage from the store, fire off the necessary API calls sequentially, and dispatch the 'NavigateToNextStage' action, keeping all that complex orchestration logic entirely out of my React components.",
  "What are the trade-offs of using Redux Toolkit versus the Context API combined with useReducer for managing the state of a dynamic React dashboard?": "I've used both, and the trade-offs are significant. Context API with `useReducer` is built into React and requires zero external dependencies. However, Context is completely unoptimized for high-frequency updates. If I put a fast-changing value in Context, *every* component consuming that Context will re-render, even if they only care about a piece of the data that didn't change. Redux Toolkit solves this. Redux sits outside the React tree, and components subscribe only to specific pieces of state via `useSelector`. If an unrelated piece of state updates, the component simply doesn't re-render. For a dynamic dashboard with constantly ticking data, Redux Toolkit is mandatory; Context API would cause massive layout thrashing.",
  "You used Next.js for building high-performance user interfaces. Can you compare SSG, SSR, ISR, and CSR? Which strategies would you choose for Domain Scanner's dashboards vs. a public-facing risk report page?": "Absolutely. CSR (Client-Side Rendering) fetches data in the browser; SSR (Server-Side Rendering) fetches data on request and sends HTML; SSG (Static Site Generation) builds HTML at compile time; and ISR (Incremental Static Regeneration) is SSG but updates the static pages in the background on a schedule. For Domain Scanner's private dashboard, which is highly interactive and behind authentication, I use CSR or SSR. For the public-facing risk reports, SEO and speed are paramount, but the data changes periodically. Therefore, I strictly use ISR. I generate the reports at build time, and set a `revalidate` time of 60 seconds. The user gets instant cached HTML from the CDN, and Next.js seamlessly rebuilds it in the background if the data changed.",
  "How did you manage authentication state and protect specific routes in Next.js, particularly for the SSO-based authentication framework you architected in Domain Scanner?": "In Domain Scanner, security was top priority. I implemented route protection at the Edge using Next.js Middleware. Instead of waiting for a React component to mount and check if a user is logged in (which causes flickering), the Middleware intercepts the request before it even hits the server. It verifies the JWT token stored securely in an HttpOnly cookie. If the token is missing or invalid, the Middleware performs a 302 redirect to the SSO login page instantly. On the client side, I used a React Context provider wrapped around the application to hold the decoded user profile so components could easily check role-based permissions without parsing the cookie.",
  "Explain the App Router vs. Pages Router in Next.js. What architectural changes would you consider if migrating Domain Scanner from Pages to App Router?": "The Pages Router is built around page-level data fetching with `getServerSideProps` and `getStaticProps`, where everything is essentially a Client Component by default. The App Router fundamentally changes this by introducing React Server Components (RSCs) as the default. If I were migrating Domain Scanner, the biggest architectural shift would be moving data fetching directly into my layout and server components using standard `async/await fetch()`, completely eliminating `getServerSideProps`. I would push 'use client' directives as far down the component tree as possible—only putting them on interactive elements like buttons or charts—which would massively reduce the JavaScript bundle size shipped to the user.",
  "For a highly dynamic SaaS application like Domain Scanner, how do you handle hydration mismatches in Next.js, and what are common causes?": "A hydration mismatch occurs when the server renders a piece of HTML (like `<p>Timestamp: 12:00</p>`), but when React boots up on the client and runs the exact same code, it generates something different (like `<p>Timestamp: 12:01</p>`). React panics because the DOM doesn't match the virtual DOM. The most common cause I've seen is relying on `window`, `localStorage`, or `Date.now()` during the initial render. To fix this, I never render browser-specific data on the first pass. I use a `useEffect` hook to set an `isMounted` state to true, and only render the client-specific data once `isMounted` is true. Alternatively, for heavy client-side charts, I use `next/dynamic` with `ssr: false` to completely skip server-side rendering for that component.",
  "How would you architect a reusable UI component library for Neutrinos to ensure consistency across different role-specific UI flows?": "To ensure consistency across Neutrinos, I architected our UI library using the Atomic Design methodology. I started by defining core tokens—colors, typography, spacing—using CSS variables. Then I built 'Atoms' like generic Buttons and Inputs. These combined into 'Molecules' like FormFields, and finally 'Organisms' like a complete StageApprovalCard. Crucially, I isolated this library completely from our business logic. The components accepted raw props and emitted events; they knew nothing about Redux or our API. We documented every component in Storybook, allowing designers and developers to visually test states and edge cases in isolation before they were ever integrated into the main application.",
  "For the complex UI flows in Neutrinos, how did you ensure the CSS remained maintainable? Did you use CSS Modules, Styled Components, or Tailwind, and what are the pros/cons of your approach?": "We evaluated all three. Styled Components offer incredible dynamic props, but the runtime javascript overhead for injecting styles was too high. Tailwind is amazing for rapid prototyping, but in our complex forms, the class strings became massive and unreadable. I ultimately chose CSS Modules combined with SASS. CSS Modules automatically generate unique class names (like `Button_btn__3f9k`), guaranteeing zero global scope collisions. It gave us the full power of SASS features like mixins and nesting, zero runtime overhead, and kept our React components extremely clean since we were just importing a style object.",
  "How do you analyze and improve the First Contentful Paint (FCP) and Largest Contentful Paint (LCP) in a Next.js application like Domain Scanner?": "I constantly profile using the Chrome Performance tab and Lighthouse to measure Web Vitals. To improve FCP, I aggressively minimize render-blocking resources. I ensure critical CSS is inlined and defer non-essential third-party scripts (like analytics) using Next.js `<Script strategy=\"lazyOnload\">`. For LCP, which in Domain Scanner was often the main data grid or hero chart, I ensure the API response is extremely fast, often caching it at the CDN edge. If the LCP is an image, I strictly use the `next/image` component with the `priority={true}` flag so the browser knows to preload that specific image immediately before parsing the rest of the DOM.",
  "How do you implement code splitting and lazy loading in React/Next.js to reduce the initial bundle size of a complex application?": "Next.js automatically handles route-level code splitting, meaning users only download the JavaScript for the specific page they navigate to. However, for a complex app, that's not enough. In our dashboard, we had a massive D3.js charting library that was only visible if the user clicked 'View Analytics'. I implemented component-level lazy loading using `next/dynamic` (or `React.lazy` with `<Suspense>`). I dynamically imported the Chart component so the D3 library was entirely excluded from the initial bundle. It was only fetched over the network exactly when the user clicked the button, drastically reducing our initial load time.",
  "Explain how you would handle complex CSS animations for the stage-based decision paths in Neutrinos without causing layout thrashing or jank.": "The golden rule for 60fps web animations is to only ever animate properties that are processed by the GPU's compositor thread, specifically `transform` and `opacity`. If you animate `width`, `height`, or `margin`, you trigger the browser's layout engine to recalculate the geometry of the entire page on every single frame, causing massive CPU spikes and jank. For Neutrinos, when a decision panel slid into view, I didn't animate its left margin; I used `transform: translateX(100%)` to `translateX(0)`. Furthermore, I added the `will-change: transform` property to tell the browser to promote that element to its own GPU layer ahead of time.",
  "What strategies do you use for optimizing images and other media assets in a high-performance Next.js frontend?": "My primary strategy is leveraging the native `next/image` component. Instead of standard `<img>` tags, `next/image` automatically performs on-demand optimization. It detects the user's browser capabilities and serves modern, highly compressed formats like WebP or AVIF. It automatically resizes the images based on the device's viewport, so a mobile user doesn't download a 4K desktop asset. Additionally, it implements native lazy-loading by default, meaning images below the fold don't consume network bandwidth until the user actually scrolls near them. For massive scale, I ensure these optimized assets are served directly from a global CDN.",
  "How do React Server Components differ fundamentally from traditional SSR, and how does the serialization of the RSC payload impact the types of props you can pass between Server and Client Components?": "Traditional SSR renders the entire component tree to HTML on the server, sends it down, and then the client downloads the full JS bundle to 'hydrate' and make it interactive. React Server Components (RSCs) are fundamentally different: they NEVER execute on the client. The server executes them and streams down a special, serialized JSON-like representation of the UI (the RSC payload). Because this data is sent over the network boundary, everything passed from a Server Component to a Client Component must be strictly serializable. This means I can pass strings, numbers, and JSON objects as props, but I absolutely cannot pass functions, callbacks, or class instances, because they cannot be serialized across the wire.",
  "Describe a scenario where a Client Component is nested inside a Server Component, which is itself nested inside another Client Component. How does React handle the render tree and module graph resolution in this interwoven structure?": "This is a classic 'interleaving' scenario. You cannot simply import a Server Component directly into a Client Component—it will throw an error because the client can't execute server code. However, I handle this by using the `children` prop. I can have an outer Client Component (like a `<Collapsible Sidebar>`), and pass a Server Component (like `<DatabaseList>`) into it as `children`. React handles this brilliantly: it executes the Server Component on the server, serializes the output, and leaves a 'hole' in the RSC payload. When the client renders the outer Client Component, it simply slots the pre-rendered server UI into that hole, allowing seamless composition without violating the module graph constraints.",
  "When implementing streaming SSR with React Suspense, how does the HTML response actually stream to the browser? How are fallback boundaries replaced with the actual content once resolved?": "With streaming SSR, we don't wait for the slowest API call to finish before sending the response. I wrap slow components in a `<Suspense>` boundary. The server immediately sends down the initial HTML shell, including the `<div class=\"spinner\">` fallback UI for the suspended component, leaving the HTTP connection open. Once the backend data resolves, React on the server renders that specific chunk to HTML. It then streams that HTML chunk down the open connection, immediately followed by a tiny inline `<script>` tag. That script essentially says, 'Hey DOM, take this new HTML and swap it with the spinner.' This happens progressively before the main React JavaScript bundle even finishes downloading.",
  "In WebGL, what is the role of the Vertex Shader versus the Fragment Shader, and how does data flow between them via varyings?": "In the WebGL pipeline, the Vertex Shader runs first. Its primary job is mathematics: it takes raw 3D coordinate data (attributes) from the CPU and transforms them into 2D clip-space coordinates on the screen. It executes exactly once per vertex. The Fragment Shader runs later during rasterization. Its job is color: it calculates the exact RGBA color for every single pixel (fragment) between those vertices. To pass data from the Vertex Shader to the Fragment Shader—like lighting normals or texture coordinates—I use variables called `varyings`. The GPU automatically interpolates these varying values across the surface of the triangle before passing them into the Fragment Shader for each specific pixel.",
  "Describe a technique to efficiently render 100,000 instances of the same 3D mesh in WebGL without overwhelming the CPU with draw calls.": "If I tried to issue 100,000 separate `gl.drawArrays` calls, the CPU-to-GPU communication overhead would completely crash the framerate. The correct technique is Instanced Rendering (using `gl.drawArraysInstanced` in WebGL 2). I upload the core geometry (vertices, normals) to the GPU exactly once. Then, I create a separate 'instanced' buffer containing the unique data for each of the 100,000 instances, like their specific X/Y/Z positions and colors. I issue a single draw call. The Vertex Shader runs, pulling the core geometry and advancing through the instanced buffer for each object. The GPU renders 100,000 objects in a single pass, completely eliminating the CPU bottleneck.",
  "What causes \"WebGL Context Loss\", how can it be simulated, and what is the proper architectural approach to recover from it in a large-scale graphics application?": "WebGL Context Loss happens when the operating system forcibly takes back GPU memory—often because the user switched tabs, updated a driver, or the GPU ran out of VRAM. It can be simulated via the WebGL extension `WEBGL_lose_context`. A robust architecture must explicitly listen for the `webglcontextlost` and `webglcontextrestored` DOM events. When context is lost, all GPU state is wiped clean. You cannot just resume rendering. The proper approach I take is to flag the application as 'paused', destroy all JavaScript references to WebGL objects, wait for the `restored` event, and then completely re-initialize the renderer from scratch—recompiling shaders, re-uploading textures, and rebuilding buffers before resuming the render loop.",
  "How does memory management work in WebAssembly when interoperating with JavaScript, and what are the specific challenges regarding passing complex objects (like strings or JSON) across the JS-Wasm boundary?": "WebAssembly doesn't have garbage collection or object references like JavaScript. It operates entirely on a single, contiguous block of raw memory—represented in JS as a flat `WebAssembly.Memory` ArrayBuffer. Wasm only understands primitive numbers (i32, f64). To pass a string from JS to Wasm, you can't just pass the string. I have to encode the JS string into UTF-8 bytes, write those bytes directly into the Wasm memory buffer at a specific memory offset, and then pass that integer offset (a pointer) and the string length to the Wasm function. Wasm reads the raw bytes from that memory address to reconstruct the string. It requires strict, manual memory management.",
  "Explain the security model of WebAssembly. Can a malicious Wasm module execute arbitrary code on the host machine or access the browser's DOM directly?": "No, a malicious Wasm module cannot execute arbitrary code or touch the DOM natively. WebAssembly was built with a strict sandbox security model. By default, a Wasm module operates in a completely isolated environment; it can only compute numbers and read/write to its own isolated memory buffer. It has exactly zero access to the DOM, the network, the filesystem, or the host OS. The *only* way Wasm can interact with the outside world is if the host JavaScript environment explicitly imports JS functions (like `console.log` or a DOM manipulation function) and passes them into the Wasm module at instantiation. Wasm is entirely constrained by the browser's Same-Origin Policy.",
  "What is the WebAssembly System Interface (WASI), and why is it significant for the future of Wasm outside the browser?": "WebAssembly is great in the browser, but what if we want to run it on a server or an edge node? Since Wasm is heavily sandboxed, it has no native way to open a file or open a network socket. WASI (WebAssembly System Interface) is a standardized API that solves this. It provides a secure, capability-based set of system calls (like reading files or getting the system time) that Wasm modules can target. Its significance is massive: it means I can compile C++, Rust, or Go into a single Wasm binary, and run that exact same binary securely on a Linux server, a Windows machine, or an Edge worker, completely independent of the browser and without writing proprietary bindings for each platform.",
  "Compare Module Federation (Webpack/Rspack) with iframe-based micro-frontends. What are the specific trade-offs regarding state sharing, routing, and CSS isolation?": "Iframe-based micro-frontends are the legacy approach. They provide perfect CSS and JS sandbox isolation, but they are terrible for user experience. Routing is deeply disjointed, and state sharing requires messy, asynchronous `postMessage` event buses. Module Federation, introduced in Webpack 5, is vastly superior. It allows a host application to dynamically load compiled JS modules from remote servers at runtime. The remotes render natively inside the host's DOM. This means they can seamlessly share React Context, global state, and routing. However, the trade-off is isolation: because they share the DOM, if Remote A writes aggressive global CSS, it will break Remote B. You have to enforce strict CSS scoping (like CSS Modules) across all federated teams.",
  "In a Module Federation setup, how do you handle dependency version conflicts when Remote A requires React 17 and Remote B requires React 18, both loaded into Host H?": "Module Federation handles this via the `shared` configuration object. You specify dependencies like React as singletons. If Host H provides React 18, and Remote A strictly requires React 17 (e.g., `requiredVersion: '17.x'`), Webpack will throw a runtime error rather than attempting to boot two conflicting versions of React in the same DOM. To resolve this architecturally, teams must either agree on a strict versioning contract (upgrading A to 18), or, if that's impossible, Remote A cannot share the singleton. It would have to bundle its own React 17 runtime, which destroys the performance benefits of Module Federation and causes hook errors if they try to share components. Strict governance on shared dependencies is required.",
  "How do you implement robust error handling and fallback UI boundaries when dynamically loading a remote micro-frontend that might be offline or failing to parse?": "When loading code over the network dynamically, you have to assume it will eventually fail. If a remote micro-frontend server goes down, and the host tries to import it, it will crash the entire host application. My approach is wrapping every federated remote import in a combination of React `<Suspense>` and a custom `<ErrorBoundary>`. If the network request is pending, Suspense shows a skeleton loader. If the remote server returns a 500 or the module fails to parse, the Error Boundary catches the crash. Instead of a white screen of death, I render a localized 'Component Unavailable' fallback UI, allowing the user to continue using the rest of the host application unimpeded.",
  "While profiling a React application in the Chrome Performance tab, you notice long \"Recalculate Style\" and \"Layout\" blocks causing dropped frames. What causes this, and how do you trace it back to the offending code?": "This is textbook 'Layout Thrashing', also known as Forced Synchronous Layout. It happens when JavaScript writes to the DOM (like changing a class name) and then immediately reads a layout-dependent property (like `element.offsetHeight`). The browser is forced to pause JavaScript execution and synchronously calculate the entire page's CSS and geometry to answer the read request. To track this down, I open the Chrome Performance tab, zoom in on the red 'Layout' task, and look at the Call Tree or the 'Initiator' link. Chrome explicitly warns you and points exactly to the JavaScript function line that requested the style read immediately after a write, allowing me to decouple the read/write operations into separate animation frames.",
  "Explain the difference between Time to Interactive (TTI), Total Blocking Time (TBT), and Interaction to Next Paint (INP). Why is INP replacing FID as a Core Web Vital?": "Time to Interactive (TTI) measures when the page is fully loaded and the main thread is idle enough to reliably respond to user input. Total Blocking Time (TBT) is a lab metric summing up all the time the main thread was blocked for more than 50ms between First Contentful Paint and TTI. Interaction to Next Paint (INP) is the modern gold standard. It measures the latency of the single worst, longest interaction (like a click or keypress) over the *entire lifespan* of the user's visit. Google replaced First Input Delay (FID) with INP because FID only measured the very first interaction during page load. A page could have a great FID but freeze constantly afterward; INP captures that poor overall responsiveness.",
  "You have a massive Redux store and notice that dispatching a single action causes significant input lag, even though the reducer is fast. What profiling techniques and optimizations do you apply?": "If the reducer is fast but dispatching causes lag, the issue is almost certainly excessive React component re-rendering due to poorly optimized subscriptions. I immediately fire up the React DevTools Profiler, record the action dispatch, and enable 'Record why each component rendered'. This quickly shows me which components re-rendered unnecessarily. The solution is threefold: First, I ensure components are selecting only the granular data they need (e.g., `useSelector(state => state.user.name)` instead of `useSelector(state => state.user)`). Second, for derived data, I implement memoized selectors using Reselect, so the reference doesn't change unless the underlying data does. Third, I wrap heavy UI components in `React.memo` to short-circuit the render tree."
};

const run = async () => {
  try {
    await rewriteAnswers('round-2', map);
    process.exit(0);
  } catch (err) {
    console.error(err);
    process.exit(1);
  }
}
run();
