import { appendDsaTopics } from './dsa-seeder-utils.js';

const topics = [
  {
    slug: "dsa-ch51-dp-memoization",
    title: "Chapter 51: Dynamic Programming - The Art of Memoization",
    order: 51,
    content: "<h2>Understanding Memoization Exhaustively</h2><p>Memoization is a Top-Down Dynamic Programming approach. The fundamental idea is to cache the results of expensive function calls and return the cached result when the same inputs occur again. Unlike Tabulation (Bottom-Up), Memoization preserves the natural recursive flow of the algorithm, making it incredibly intuitive for problems involving trees, graphs, and complex state transitions.</p><h3>The Core Mechanics</h3><p>At its heart, Dynamic Programming requires two properties to be applicable:</p><ul><li><strong>Overlapping Subproblems:</strong> The recursive algorithm repeatedly solves the same subproblems.</li><li><strong>Optimal Substructure:</strong> An optimal solution to the problem contains optimal solutions to the subproblems.</li></ul><p>Without Overlapping Subproblems, Memoization is useless (this is where Divide & Conquer typically sits, like Merge Sort). Without Optimal Substructure, DP cannot guarantee a globally optimal result (this is where Backtracking/Heuristics come in).</p><h3>Edge Cases & Common Pitfalls</h3><p>A common mistake in Memoization is hashing the state incorrectly. In JavaScript, using an array or object as a key directly can lead to unintended reference matches instead of value matches. Always serialize multiple parameters into a primitive string key, like <code>`${param1},${param2}`</code>, or use a multi-dimensional array if the bounds are strictly known.</p><h3>Example 1: Fibonacci Sequence (Brute Force vs Optimized)</h3><h4>Brute Force (Exponential Time)</h4><pre><code>function fibBrute(n) {\n  if (n <= 1) return n;\n  return fibBrute(n - 1) + fibBrute(n - 2);\n}\n// Time Complexity: O(2^n)\n// Space Complexity: O(n) due to the call stack.</code></pre><h4>Optimized Memoization (Linear Time)</h4><pre><code>function fibMemo(n, memo = {}) {\n  if (n in memo) return memo[n];\n  if (n <= 1) return n;\n  memo[n] = fibMemo(n - 1, memo) + fibMemo(n - 2, memo);\n  return memo[n];\n}\n// Time Complexity: O(n)\n// Space Complexity: O(n) for the call stack + O(n) for the memo map.</code></pre><h3>Visual State Tree (Fibonacci)</h3><pre><code>// Un-memoized fib(5) tree:\n//                     fib(5)\n//                 /          \\\n//             fib(4)         fib(3)\n//            /      \\        /     \\\n//        fib(3)    fib(2) fib(2)  fib(1)\n//       /    \\      /   \\\n//    fib(2) fib(1) fib(1) fib(0)\n// As you can see, fib(3) is computed twice, fib(2) three times. Memoization prunes these redundant branches immediately.</code></pre>",
    interviewQuestions: [
      { question: "When should you prefer Memoization over Tabulation?", answer: "Memoization is preferred when the state space is sparse. If you only need to visit a fraction of the possible states to reach the solution, Tabulation might waste time computing unreachable states, whereas Memoization strictly computes only what is needed on demand." },
      { question: "What is the primary danger of using Memoization in languages without Tail Call Optimization (like standard Node.js/V8)?", answer: "The primary danger is a Stack Overflow (Maximum call stack size exceeded). Since Memoization relies heavily on deep recursion, extremely large inputs can blow the stack, whereas Tabulation avoids recursion entirely." },
      { question: "How do you handle multi-parameter states in a memoization cache?", answer: "Serialize the parameters into a unique string key, e.g., `const key = row + ',' + col`. Alternatively, use a 2D/3D array if the state dimensions are small and fixed." },
      { question: "Can Memoization be used to optimize algorithms that don't have overlapping subproblems?", answer: "No. If every subproblem is unique (e.g., standard Merge Sort), a memoization cache will never get a 'cache hit', resulting in wasted memory and time overhead for maintaining the hash map." },
      { question: "Is the Space Complexity of Memoization always worse than Tabulation?", answer: "Generally, yes, because Memoization incurs the overhead of the recursive call stack plus the hash map structure, whereas Tabulation can often be optimized to use minimal space (e.g., storing just the last 2 variables in Fibonacci)." }
    ],
    practicalTask: {
      scenario: "Climbing Stairs (Variations).",
      task: "You are climbing a staircase. It takes n steps to reach the top. Each time you can either climb 1 or 2 steps. Provide a memoized solution. Include Time/Space complexity.",
      solutionCode: "function climbStairs(n, memo = {}) {\n  if (n in memo) return memo[n];\n  if (n === 1) return 1;\n  if (n === 2) return 2;\n  memo[n] = climbStairs(n - 1, memo) + climbStairs(n - 2, memo);\n  return memo[n];\n}\n// Time Complexity: O(n), Space Complexity: O(n)"
    }
  },
  {
    slug: "dsa-ch52-dp-tabulation",
    title: "Chapter 52: Dynamic Programming - Tabulation vs Memoization",
    order: 52,
    content: "<h2>Bottom-Up Mastery: Tabulation</h2><p>Tabulation is the Bottom-Up approach to Dynamic Programming. Instead of starting from the target state and recursively asking for subproblems, we start from the smallest base cases and iteratively compute the answers to larger subproblems using loops and an array (or matrix) until we reach the target state.</p><h3>Why Tabulate?</h3><p>Tabulation entirely eliminates the overhead of recursion, avoiding stack overflows. Furthermore, it often allows for extreme Space Complexity optimizations (State Reduction). For example, if determining state <code>i</code> only requires state <code>i-1</code> and <code>i-2</code>, you do not need an array of size N; you only need two variables.</p><h3>Edge Cases & Pitfalls</h3><p>A common pitfall in Tabulation is incorrectly initializing the base cases or the DP table. Always ensure your DP array is sized appropriately (often <code>N+1</code>) and pre-filled with values that won't interfere with your minimization/maximization logic (e.g., <code>Infinity</code> for minimums, <code>0</code> or <code>-Infinity</code> for maximums).</p><h3>Example: Minimum Path Sum</h3><h4>Memoization (Top-Down)</h4><pre><code>function minPathSumMemo(grid) {\n  const ROWS = grid.length, COLS = grid[0].length;\n  const memo = new Map();\n  const dfs = (r, c) => {\n    if (r === ROWS - 1 && c === COLS - 1) return grid[r][c];\n    if (r >= ROWS || c >= COLS) return Infinity;\n    const key = `${r},${c}`;\n    if (memo.has(key)) return memo.get(key);\n    const res = grid[r][c] + Math.min(dfs(r + 1, c), dfs(r, c + 1));\n    memo.set(key, res);\n    return res;\n  };\n  return dfs(0, 0);\n}\n// Time: O(M*N), Space: O(M*N)</code></pre><h4>Tabulation (Bottom-Up)</h4><pre><code>function minPathSumTab(grid) {\n  const ROWS = grid.length, COLS = grid[0].length;\n  const dp = Array.from({length: ROWS}, () => Array(COLS).fill(0));\n  dp[0][0] = grid[0][0];\n  \n  for (let i = 1; i < ROWS; i++) dp[i][0] = dp[i-1][0] + grid[i][0];\n  for (let j = 1; j < COLS; j++) dp[0][j] = dp[0][j-1] + grid[0][j];\n  \n  for (let r = 1; r < ROWS; r++) {\n    for (let c = 1; c < COLS; c++) {\n      dp[r][c] = grid[r][c] + Math.min(dp[r-1][c], dp[r][c-1]);\n    }\n  }\n  return dp[ROWS-1][COLS-1];\n}\n// Time: O(M*N), Space: O(M*N) (Can be optimized to O(N))</code></pre>",
    interviewQuestions: [
      { question: "How can you optimize the space complexity of Tabulation?", answer: "By observing the recurrence relation. If computing `dp[i]` only depends on `dp[i-1]` and `dp[i-2]`, we don't need a full array of size N. We just need two variables holding the previous two states, reducing space from O(N) to O(1)." },
      { question: "In what scenario is Tabulation worse than Memoization?", answer: "When the state space is massive but only a few states are actually reachable or needed. Tabulation strictly fills every cell in the DP table from bottom to top, computing unreachable states. Memoization only computes exactly what is requested." },
      { question: "Why do we often use size `N+1` for our DP arrays?", answer: "To easily handle base cases or 'empty' states. For example, in string matching or knapsack, index 0 might represent an empty string or 0 capacity, allowing the main loop to cleanly run from 1 to N without out-of-bounds errors on `i-1`." },
      { question: "What is topological sorting and how does it relate to Tabulation?", answer: "Tabulation requires computing states in a topological order (dependencies computed before dependents). In a grid, scanning top-to-bottom, left-to-right guarantees that the top and left cells are computed before the current cell." },
      { question: "Can all Memoization solutions be converted to Tabulation?", answer: "Theoretically, yes. Any algorithm expressed as a recursive DP with a DAG (Directed Acyclic Graph) of states can be flattened into a bottom-up iterative approach, provided you can determine a valid topological ordering for the loops." },
      { question: "What happens if there are cycles in the state transitions?", answer: "Standard DP (both Tabulation and Memoization) will fail or infinitely loop if there are cyclic dependencies. Cycles mean it's a general graph problem, requiring algorithms like Dijkstra's or Bellman-Ford, not pure DP." }
    ],
    practicalTask: {
      scenario: "House Robber (Space Optimized).",
      task: "You are a professional robber planning to rob houses along a street. You cannot rob adjacent houses. Given an integer array nums, return the maximum amount of money you can rob tonight without alerting the police.",
      solutionCode: "function rob(nums) {\n  if (nums.length === 0) return 0;\n  let rob1 = 0, rob2 = 0;\n  // [rob1, rob2, n, n+1, ...]\n  for (let num of nums) {\n    let temp = Math.max(num + rob1, rob2);\n    rob1 = rob2;\n    rob2 = temp;\n  }\n  return rob2;\n}\n// Time: O(N), Space: O(1)"
    }
  },
  {
    slug: "dsa-ch53-dp-knapsack",
    title: "Chapter 53: Dynamic Programming - The 0/1 Knapsack Problem",
    order: 53,
    content: "<h2>The Mother of all DP Problems</h2><p>The 0/1 Knapsack problem is the prototypical example of multi-dimensional Dynamic Programming. You are given a knapsack with a maximum weight capacity <code>W</code>, and a set of items, each with a <code>weight</code> and a <code>value</code>. You must maximize the value in the knapsack without exceeding the capacity. The '0/1' means you either take an item completely (1) or leave it completely (0). You cannot take fractions.</p><h3>The Theory</h3><p>The state depends on two variables: the current item index <code>i</code> and the remaining capacity <code>w</code>. For each item, we have two choices:</p><ul><li><strong>Skip it:</strong> Max value is the same as the previous items with the same capacity: <code>dp[i-1][w]</code></li><li><strong>Take it (if it fits):</strong> Max value is the item's value plus the best we could do with the remaining capacity using previous items: <code>values[i] + dp[i-1][w - weights[i]]</code></li></ul><p>We take the maximum of these two choices.</p><h3>Example: Brute Force vs DP</h3><h4>Brute Force Recursion</h4><pre><code>function knapsackBrute(weights, values, capacity, i) {\n  if (i < 0 || capacity === 0) return 0;\n  \n  if (weights[i] > capacity) {\n    return knapsackBrute(weights, values, capacity, i - 1);\n  }\n  \n  const include = values[i] + knapsackBrute(weights, values, capacity - weights[i], i - 1);\n  const exclude = knapsackBrute(weights, values, capacity, i - 1);\n  \n  return Math.max(include, exclude);\n}\n// Time: O(2^N), Space: O(N) stack</code></pre><h4>Tabulation</h4><pre><code>function knapsackDP(weights, values, capacity) {\n  const N = weights.length;\n  const dp = Array.from({length: N + 1}, () => Array(capacity + 1).fill(0));\n  \n  for (let i = 1; i <= N; i++) {\n    for (let w = 1; w <= capacity; w++) {\n      if (weights[i-1] <= w) {\n        dp[i][w] = Math.max(dp[i-1][w], values[i-1] + dp[i-1][w - weights[i-1]]);\n      } else {\n        dp[i][w] = dp[i-1][w];\n      }\n    }\n  }\n  return dp[N][capacity];\n}\n// Time: O(N * W), Space: O(N * W)</code></pre>",
    interviewQuestions: [
      { question: "What is the difference between 0/1 Knapsack and Unbounded Knapsack?", answer: "In 0/1, an item can be picked at most once. In Unbounded (like Coin Change), an item can be picked infinite times. In DP terms, when taking an item in Unbounded, you reuse the current item index `i` instead of moving to `i-1`." },
      { question: "Is the Knapsack problem solvable in Polynomial Time?", answer: "No, it is NP-Complete. The DP solution is 'Pseudo-Polynomial'. The time complexity is O(N * W), where W is the numeric value of the capacity. If W is extremely large, the algorithm is essentially exponential relative to the number of bits required to represent W." },
      { question: "How can we optimize the space complexity of the 0/1 Knapsack DP solution?", answer: "Since `dp[i]` only depends on `dp[i-1]`, we can reduce the 2D array to a 1D array of size `W + 1`. We must iterate the capacity backwards (from W down to 0) to avoid using the same item multiple times." },
      { question: "Why do we iterate backwards when optimizing 0/1 Knapsack space?", answer: "If we iterate forwards, calculating `dp[w]` might use `dp[w - weight]` which was just updated in the current step `i`, effectively allowing an item to be picked multiple times. Iterating backwards ensures we only use values from the previous step `i-1`." },
      { question: "What is the Subset Sum problem and how does it relate to Knapsack?", answer: "Subset Sum asks if a subset of elements sums to exactly a target value. It's identical to 0/1 Knapsack where the `weight` and `value` of each item are exactly the same, and we are checking if the max value equals the target capacity." }
    ],
    practicalTask: {
      scenario: "Partition Equal Subset Sum.",
      task: "Given an integer array nums, return true if you can partition the array into two subsets such that the sum of the elements in both subsets is equal. This is a classic 0/1 Knapsack variation where the target capacity is Total Sum / 2.",
      solutionCode: "function canPartition(nums) {\n  let totalSum = nums.reduce((a, b) => a + b, 0);\n  if (totalSum % 2 !== 0) return false;\n  let target = totalSum / 2;\n  \n  let dp = new Set([0]);\n  for (let num of nums) {\n    let nextDp = new Set(dp);\n    for (let t of dp) {\n      if (t + num === target) return true;\n      nextDp.add(t + num);\n    }\n    dp = nextDp;\n  }\n  return false;\n}"
    }
  },
  {
    slug: "dsa-ch54-backtracking-generation",
    title: "Chapter 54: Backtracking Mastery - Generating All Possibilities",
    order: 54,
    content: "<h2>Exploring the Unknown</h2><p>Backtracking is an algorithmic technique for solving problems recursively by trying to build a solution incrementally, one piece at a time, and removing those solutions that fail to satisfy the constraints of the problem at any point in time.</p><h3>The General Template</h3><p>Almost all backtracking problems can be solved with a standard template:</p><pre><code>function backtrack(state, choices) {\n  if (isComplete(state)) {\n    result.push([...state]); // Push a deep copy\n    return;\n  }\n  for (let choice of choices) {\n    if (isValid(choice, state)) {\n      state.push(choice); // Do\n      backtrack(state, choices); // Recurse\n      state.pop(); // Undo (The actual 'backtracking' step)\n    }\n  }\n}</code></pre><h3>Permutations vs Combinations vs Subsets</h3><ul><li><strong>Permutations:</strong> Order matters (<code>[1,2] != [2,1]</code>). We use all items. Iterate from 0 every time, but skip already used items (often using a boolean <code>used</code> array).</li><li><strong>Combinations:</strong> Order doesn't matter, fixed size. Iterate forward using an <code>index</code> pointer to prevent duplicates.</li><li><strong>Subsets:</strong> Order doesn't matter, variable size. Every node in the recursion tree is a valid subset.</li></ul><h3>Example: Permutations</h3><pre><code>function permute(nums) {\n  const result = [];\n  \n  const dfs = (path, used) => {\n    if (path.length === nums.length) {\n      result.push([...path]); // Crucial: create a copy\n      return;\n    }\n    for (let i = 0; i < nums.length; i++) {\n      if (used[i]) continue;\n      \n      path.push(nums[i]);\n      used[i] = true;\n      \n      dfs(path, used);\n      \n      // Backtrack\n      path.pop();\n      used[i] = false;\n    }\n  };\n  \n  dfs([], Array(nums.length).fill(false));\n  return result;\n}\n// Time Complexity: O(N! * N)\n// Space Complexity: O(N) call stack</code></pre>",
    interviewQuestions: [
      { question: "Why do we push a copy of the path array (e.g., `[...path]`) into the result instead of just `path`?", answer: "Because arrays are passed by reference. If you push the `path` reference directly, future recursive calls and backtracking operations (like `pop()`) will mutate the array you just pushed. Pushing a copy takes a snapshot of the array at that exact moment." },
      { question: "What is the Time Complexity of generating all subsets of an array of size N?", answer: "O(N * 2^N). There are 2^N possible subsets, and creating a copy of each subset takes O(N) time in the worst case." },
      { question: "How does Backtracking differ from pure Depth First Search (DFS)?", answer: "DFS is a traversal algorithm generally used on graphs/trees. Backtracking is a problem-solving technique that often uses DFS to explore a 'state-space tree', but involves actively reverting state (undoing choices) to explore other branches." },
      { question: "How do you handle generating subsets/permutations when the input array contains duplicates?", answer: "First, sort the input array so duplicates are adjacent. During iteration, if `nums[i] === nums[i-1]` and the previous identical element was NOT used in the current recursive step (or was skipped), we `continue` to avoid duplicate branches." },
      { question: "What is the Space Complexity of Backtracking algorithms?", answer: "Usually O(H) where H is the maximum depth of the recursion tree (the size of a single path). However, if you are required to return all results, storing them will dominate memory (e.g., O(N * N!) for permutations)." }
    ],
    practicalTask: {
      scenario: "Combinations.",
      task: "Given two integers n and k, return all possible combinations of k numbers chosen from the range [1, n].",
      solutionCode: "function combine(n, k) {\n  const res = [];\n  const backtrack = (start, path) => {\n    if (path.length === k) {\n      res.push([...path]);\n      return;\n    }\n    for (let i = start; i <= n; i++) {\n      path.push(i);\n      backtrack(i + 1, path);\n      path.pop();\n    }\n  };\n  backtrack(1, []);\n  return res;\n}"
    }
  },
  {
    slug: "dsa-ch55-backtracking-pruning",
    title: "Chapter 55: Backtracking Optimization - Pruning the State-Space Tree",
    order: 55,
    content: "<h2>Cutting the Dead Branches</h2><p>In standard backtracking, we systematically explore the entire state space. However, in many problems, we can mathematically prove that certain branches will never lead to a valid or optimal solution. Abandoning these branches early is called <strong>Pruning</strong>.</p><h3>The N-Queens Problem</h3><p>The N-Queens puzzle is the classic example of Pruning. You must place N queens on an NxN chessboard such that no two queens threaten each other. Instead of generating all possible board layouts and validating them (which is O(N^(N^2))), we place queens row by row. If placing a queen in column C on row R puts it under attack from a previously placed queen, we completely skip this column and move on.</p><h3>Pruning Mechanisms</h3><p>In N-Queens, we maintain three sets to achieve O(1) conflict detection:</p><ul><li><code>cols</code>: Columns currently occupied.</li><li><code>posDiag</code>: Positive diagonals (R + C remains constant).</li><li><code>negDiag</code>: Negative diagonals (R - C remains constant).</li></ul><h3>Example: N-Queens Solutions Count</h3><pre><code>function totalNQueens(n) {\n  let count = 0;\n  const cols = new Set(), posDiag = new Set(), negDiag = new Set();\n  \n  const backtrack = (r) => {\n    if (r === n) {\n      count++;\n      return;\n    }\n    for (let c = 0; c < n; c++) {\n      if (cols.has(c) || posDiag.has(r + c) || negDiag.has(r - c)) continue;\n      \n      // Do\n      cols.add(c); posDiag.add(r + c); negDiag.add(r - c);\n      \n      // Recurse\n      backtrack(r + 1);\n      \n      // Undo (Backtrack)\n      cols.delete(c); posDiag.delete(r + c); negDiag.delete(r - c);\n    }\n  };\n  backtrack(0);\n  return count;\n}\n// Time Complexity: O(N!) in the worst case, but significantly faster in practice due to aggressive pruning.\n// Space: O(N)</code></pre>",
    interviewQuestions: [
      { question: "What is 'Branch and Bound'?", answer: "Branch and Bound is a variation of backtracking used for optimization problems (like Traveling Salesman). As you explore branches, you keep track of the best global solution found so far. If a branch's 'best possible outcome' (the bound) is worse than the current global best, you prune it immediately." },
      { question: "How does sorting the input array help with pruning?", answer: "Sorting is a powerful prerequisite for pruning. For example, in Combination Sum, if you sort the candidates, you can immediately break out of a loop the moment `currentSum + candidates[i]` exceeds the target, because all subsequent candidates will also exceed the target." },
      { question: "Why do positive diagonals share `r + c` and negative diagonals share `r - c` in a 2D matrix?", answer: "This is a geometric property of 2D grids. As you move top-right (negative diagonal), row decreases by 1 and column increases by 1, so `r - c` remains constant. As you move bottom-right (positive diagonal), both row and column increase by 1, so `r + c` remains constant (Wait, typically bottom-right is `r - c`, top-right is `r + c` depending on origin. Either way, their sums/differences map out the diagonals!)." },
      { question: "Can Pruning improve the theoretical worst-case time complexity of Backtracking algorithms?", answer: "Sometimes it does not change the Big-O worst-case complexity (e.g., generating permutations with duplicates is still exponential in worst case), but in practice, pruning reduces the constant factors immensely, turning an algorithm that would take years to run into one that runs in milliseconds." },
      { question: "What is the Sudoku Solver algorithm an example of?", answer: "It is an advanced example of Backtracking with Pruning. We try numbers 1-9 in empty cells, but immediately prune any numbers that violate Sudoku rules for the current row, column, or 3x3 sub-box." }
    ],
    practicalTask: {
      scenario: "Sudoku Solver.",
      task: "Write a program to solve a Sudoku puzzle by filling the empty cells. Empty cells are indicated by the character '.'. Use backtracking with aggressive pruning to ensure the solver runs quickly.",
      solutionCode: "function solveSudoku(board) {\n  const isValid = (r, c, char) => {\n    for (let i = 0; i < 9; i++) {\n      if (board[r][i] === char) return false;\n      if (board[i][c] === char) return false;\n      if (board[3 * Math.floor(r / 3) + Math.floor(i / 3)][3 * Math.floor(c / 3) + i % 3] === char) return false;\n    }\n    return true;\n  };\n  \n  const solve = () => {\n    for (let r = 0; r < 9; r++) {\n      for (let c = 0; c < 9; c++) {\n        if (board[r][c] === '.') {\n          for (let char of ['1','2','3','4','5','6','7','8','9']) {\n            if (isValid(r, c, char)) {\n              board[r][c] = char;\n              if (solve()) return true;\n              board[r][c] = '.';\n            }\n          }\n          return false; // Crucial: if no valid number fits, trigger backtracking\n        }\n      }\n    }\n    return true; // Solved\n  };\n  solve();\n}"
    }
  }
];

appendDsaTopics(topics);
